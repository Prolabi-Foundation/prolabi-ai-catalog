{
  "name": "@prolabi/provider-catalog-operations",
  "version": "0.1.0",
  "private": true,
  "description": "Fail-closed publication controls for the Prolabi provider model catalog.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "engines": {
    "node": ">=24.14.0 <27",
    "npm": ">=11.0.0"
  },
  "scripts": {
    "check": "node scripts/check-repository.mjs",
    "consumer:fingerprint": "node scripts/fingerprint-desktop-authority.mjs",
    "fixture:generate": "node scripts/generate-synthetic-payload.mjs",
    "release:inspect": "node scripts/inspect-release-candidate.mjs",
    "test": "node --test test/*.test.mjs && npm run check",
    "validate:consumer": "node scripts/validate-with-desktop.mjs"
  }
}
